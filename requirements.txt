# Supplemental pip dependencies for the PTP1B causal QSAR pipeline.
#
# Recommended path: use `environment.yml` (Conda) to install the full stack,
# especially RDKit, which is strongly recommended from conda-forge.
# This file is for pip-only or mixed Conda+pip workflows.

hydra-core>=1.3
omegaconf>=2.3
python-dotenv>=1.0
openpyxl>=3.1
xlsxwriter>=3.2
pytest>=8.0
black>=24.0
ruff>=0.3
wandb>=0.16
einops>=0.7
torch-geometric>=2.5
